<h1>Reviews</h2>

<% if user_signed_in? %>
  <%= link_to "New Review", new_review_path %>
<% end %>

<% @reviews.each do |review| %>
  "<%= review.summary %>" -<%= review.name %> (<%= review.date.strftime(%m %d %y) %>)
<% end %>