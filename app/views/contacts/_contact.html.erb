<tr>
  <td><%= contact.name %></td>
  <td><%= contact.email %></td>
  <td><%= contact.message %></td>
  <td><%= contact.created_at.strftime("%m/%d/%Y") %></td>
  <td><%= contact.responded ? 'Yes' : 'No' %></td>
</tr>